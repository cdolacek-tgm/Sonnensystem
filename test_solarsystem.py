from unittest import TestCase
from solarsystem import *

__author__ = 'Cafer Özer'

class TestSolarsystem(TestCase):
    def test_InitGL(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)

    def test_refresh(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)

    def test_showPlanets(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)

    def test_keyPressed(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)

    def test_mouse(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)

    def test_main(self):
        """
        Überprüft ob das Objekt "Test" wirklich ein Objekt der Klasse Solarsystem ist
        """
        test = Solarsystem()
        self.assertIsInstance(test, Solarsystem)